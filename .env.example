# ─── Discord ───────────────────────────────────────────────
# Bot token from the Discord Developer Portal (Bot tab → Reset/Copy Token).
# The bot only needs to be invited to your server with "Send Messages" permission.
# No privileged intents (Message Content / Presence) are required.
DISCORD_BOT_TOKEN=

# ─── Steam 동접자 알림 (optional) ──────────────────────────
# STEAM_ALERT_CHANNEL_ID 를 채우면 활성화. 비워두면 기능 꺼짐.
# 동접이 직전 알림값보다 THRESHOLD 이상 "증가"하고, 또한 현재값이
# MIN_COUNT 이상일 때만 SteamDB 링크가 담긴 임베드로 "돌파" 알림을 보냅니다.
# (감소 시엔 알림 없이 기준선만 조용히 내려갑니다.)
STEAM_ALERT_CHANNEL_ID=
STEAM_APP_ID=3088400
STEAM_GAME_NAME=Deadly Trick
STEAM_POLL_INTERVAL_SEC=600
STEAM_ALERT_THRESHOLD=25
STEAM_ALERT_MIN_COUNT=5

# ─── Twitch 카테고리 라이브 알림 (optional) ────────────────
# TWITCH_CLIENT_ID + TWITCH_CLIENT_SECRET 를 채우면 활성화. 비워두면 기능 꺼짐.
# Twitch 개발자 콘솔(https://dev.twitch.tv/console/apps)에서 앱 등록 → Client Type "Confidential"
# 로 만들면 ID/Secret 이 나온다. (OAuth Redirect URL 은 http://localhost 아무거나, 실제로 안 씀.)
# 지정 카테고리에 새 방송이 켜지면 해당 채널로 "{채널이름}님이 {카테고리} 방송중!" 임베드를 보낸다.
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
TWITCH_CATEGORY_NAME=Deadly Trick
# 알림 채널 (비우면 STEAM_ALERT_CHANNEL_ID 와 같은 채널 사용)
TWITCH_ALERT_CHANNEL_ID=
# 카테고리 이름으로 자동 조회되지만, 고정하고 싶으면 game_id 직접 지정 가능
TWITCH_GAME_ID=
TWITCH_POLL_INTERVAL_SEC=600
# 시청자 N명 이상인 방송만 알림 (0 = 전부)
TWITCH_ALERT_MIN_VIEWERS=50

# ─── 치지직(CHZZK) 카테고리 라이브 알림 (optional) ─────────
# CHZZK_CLIENT_ID + CHZZK_CLIENT_SECRET 를 채우면 활성화. 비워두면 기능 꺼짐.
# 치지직 개발자센터(https://developers.chzzk.naver.com)에서 앱 등록 후 발급.
# ⚠️ 공식 Open API엔 카테고리 필터가 없어, 전체 라이브 "시청자수 상위 CHZZK_MAX_PAGES×20개"만
#    훑어서 CHZZK_CATEGORY_ID 와 일치하는 방송을 찾는다. 시청자가 적은 소규모 카테고리는
#    상위권에 안 떠서 놓칠 수 있다(공식 API 한계).
CHZZK_CLIENT_ID=
CHZZK_CLIENT_SECRET=
# 카테고리 식별자 — 치지직 카테고리 URL의 마지막 경로. 예: .../category/GAME/Deadly_Trick → Deadly_Trick
CHZZK_CATEGORY_ID=Deadly_Trick
CHZZK_CATEGORY_NAME=Deadly Trick
# 알림 채널 (비우면 STEAM_ALERT_CHANNEL_ID 와 같은 채널 사용)
CHZZK_ALERT_CHANNEL_ID=
CHZZK_POLL_INTERVAL_SEC=600
# 시청자 N명 이상인 방송만 알림 (0 = 전부)
CHZZK_ALERT_MIN_VIEWERS=50
# 매 폴링마다 훑을 라이브 페이지 수 (1페이지=20개). 클수록 더 깊게 찾지만 요청 늘어남.
CHZZK_MAX_PAGES=15

# ─── YouTube 라이브 알림 (optional) ────────────────────────
# YOUTUBE_API_KEY 를 채우면 활성화. 비워두면 기능 꺼짐.
# Google Cloud Console → 프로젝트 생성 → "YouTube Data API v3" 사용 설정 → 사용자 인증 정보
# → API 키. 공개 데이터 읽기 전용이라 OAuth 는 필요 없다.
# ⚠️ search.list 는 하루 100회가 한도(다른 엔드포인트의 10,000 unit 풀과 별개)라
#    폴링 주기를 900초(15분)보다 짧게 두면 그날 쿼터를 태워버린다. 기본값을 유지할 것.
YOUTUBE_API_KEY=
# 검색어 — YouTube 의 q 파라미터. 큰따옴표는 구문 검색, | 는 OR. 여러 표기를 묶어도 호출 1회.
YOUTUBE_SEARCH_QUERY="Deadly Trick"|"데들리 트릭"|"デッドリートリック"
# 오탐 필터 — 검색이 물어온 방송의 제목/설명에 이 중 하나도 없으면 버린다(쉼표 구분, 비우면 필터 없음)
YOUTUBE_MATCH_TERMS=Deadly Trick,데들리 트릭,デッドリートリック
YOUTUBE_CATEGORY_NAME=Deadly Trick
# 알림 채널 (비우면 STEAM_ALERT_CHANNEL_ID 와 같은 채널 사용)
YOUTUBE_ALERT_CHANNEL_ID=
YOUTUBE_POLL_INTERVAL_SEC=900
# 시청자 N명 이상인 방송만 알림 (0 = 전부)
YOUTUBE_ALERT_MIN_VIEWERS=50

# ─── 즌다몬 TTS (optional) ─────────────────────────────────
# 아래 두 엔진 URL 중 하나를 채우면 /say 슬래시 명령어가 활성화된다. 둘 다 비우면 기능 꺼짐.
# 둘 다 채우면 한국어를 제대로 읽는 GPT-SoVITS 가 선택된다.
#
#   GPT-SoVITS  진짜 한국어 발음 (다국어 모델). GPU 필요.
#   VOICEVOX    일본어 전용 → 한글을 가타카나로 음차. GPU 없이 CPU 만으로 돈다.
#
# ⚠️ 어느 쪽이든 e2-micro(1GB)에는 안 올라간다. 알림 봇과 같은 VM 에 두려면 인스턴스를
#    키우거나, 엔진만 다른 호스트(집 PC 등)에 두고 그 주소를 여기 적어야 한다.

# ── GPT-SoVITS (권장 — 진짜 한국어) ──
# https://github.com/zunzun999/zundamon-speech-webui 의 안내대로 모델을 받은 뒤,
# WebUI 대신 동봉된 API 서버를 띄운다:
#   python api_v2.py -a 0.0.0.0 -p 9880
GPT_SOVITS_BASE_URL=
# few-shot 음성 복제라 참조 음성이 있어야 목소리가 정해진다. 위 저장소의
# reference/reference.wav 를 그대로 쓰면 된다.
# ⚠️ 경로는 봇이 아니라 **API 서버 쪽 파일 시스템** 기준이다.
GPT_SOVITS_REF_AUDIO_PATH=
# 그 참조 음성이 실제로 읽고 있는 문장 (reference/ref_text.txt 의 내용)
GPT_SOVITS_PROMPT_TEXT=流し切りが完全に入ればデバフの効果が付与される
# 참조 음성의 언어. 즌다몬 참조본은 일본어다.
GPT_SOVITS_PROMPT_LANG=all_ja
# 읽을 텍스트의 언어. all_ko=한국어, ko=한영 혼용(영어 낱말이 자주 섞이면 이쪽)
GPT_SOVITS_TEXT_LANG=all_ko
# 말하기 속도 (1.0 = 기본)
GPT_SOVITS_SPEED_FACTOR=1.0
# 합성 요청 타임아웃(초). GPU 추론이라 VOICEVOX 보다 훨씬 오래 걸린다.
GPT_SOVITS_TIMEOUT_SEC=60

# ── VOICEVOX (GPU 없이 쓸 때) ──
# 도커로 띄우는 게 제일 간단하다:
#   docker run -d --restart unless-stopped -p 127.0.0.1:50021:50021 \
#     --name voicevox voicevox/voicevox_engine:cpu-latest
# ⚠️ 일본어 전용 엔진이라 봇이 한글을 가타카나로 음차해서 넘긴다.
#    "일본인이 한국어를 읽는" 발음이 나는 건 그 때문이다.
VOICEVOX_BASE_URL=
# 화자 id. 3 = 즌다몬(노멀). 1=あまあま, 7=ツンツン, 5=セクシー, 22=ささやき, 38=ヒソヒソ
# 엔진의 /speakers 를 열어보면 전체 목록을 볼 수 있다.
VOICEVOX_SPEAKER=3
# 말하기 속도 (0.5~2.0). 한국어를 음차하면 모라가 늘어 길어지므로 1.1~1.2 도 괜찮다.
VOICEVOX_SPEED_SCALE=1.0
# 음높이 (-0.15~0.15), 억양 세기 (0~2), 음량 (0~2)
VOICEVOX_PITCH_SCALE=0.0
VOICEVOX_INTONATION_SCALE=1.0
VOICEVOX_VOLUME_SCALE=1.0
# 합성 요청 타임아웃(초)
VOICEVOX_TIMEOUT_SEC=20
# 한 번에 읽을 최대 글자 수 (넘으면 잘라서 읽는다)
TTS_MAX_LENGTH=200
# 이 시간(초) 동안 아무 말도 없으면 음성 채널에서 알아서 나간다 (0 = 안 나감)
TTS_IDLE_TIMEOUT_SEC=300
# 슬래시 명령어를 이 길드에만 등록 (즉시 반영). 비우면 전역 등록 — 반영까지 최대 1시간.
TTS_COMMAND_GUILD_ID=
# "say: 안녕" 처럼 메시지로도 부르고 싶을 때만 접두사를 적는다.
# ⚠️ 이걸 켜면 디스코드 포털에서 MESSAGE CONTENT INTENT 를 켜야 한다.
#    슬래시 명령어(/say)만 쓰면 특권 인텐트가 전혀 필요 없다.
TTS_MESSAGE_PREFIX=
